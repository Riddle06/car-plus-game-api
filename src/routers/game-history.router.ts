import { Router } from "express";
import { RequestExtension } from "view-models/extension";

const router = Router();

router.get('/', async (req: RequestExtension, res, next) => {

})

router.get('/:id', async (req: RequestExtension, res, next) => {

})



export default router;